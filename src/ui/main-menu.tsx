/**
 * Main Menu — Comic panel landing page (v3.0).
 *
 * Three comic panels with character sprites and speech bubbles.
 * All game action handlers live in ComicLanding.
 */

import { ComicLanding } from './comic-landing';

export function MainMenu() {
  return <ComicLanding />;
}
