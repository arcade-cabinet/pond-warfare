/**
 * ActionsTab — Wraps the existing ActionPanel for the command panel.
 *
 * Shows train/build/tech sub-tabs with context-sensitive action buttons
 * and the training queue.
 */

import { ActionPanel } from '../action-panel';

export function ActionsTab() {
  return <ActionPanel />;
}
